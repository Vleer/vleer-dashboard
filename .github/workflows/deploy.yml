name: Build and Deploy to Microk8s

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: vleer/vleer-dashboard

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
      - run: npm run build

  build:
    needs: verify
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Determine branch and image tag
        id: meta
        run: |
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "tag=latest" >> $GITHUB_OUTPUT
            echo "deployment=dashboard" >> $GITHUB_OUTPUT
          else
            echo "tag=develop" >> $GITHUB_OUTPUT
            echo "deployment=dashboard-develop" >> $GITHUB_OUTPUT
          fi
          # Convert to lowercase for GHCR
          REGISTRY_IMAGE=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')
          echo "registry_image=${REGISTRY_IMAGE}" >> $GITHUB_OUTPUT

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: ${{ steps.meta.outputs.registry_image }}:${{ steps.meta.outputs.tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summary
        run: |
          echo "### Build Summary :rocket:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ steps.meta.outputs.registry_image }}:${{ steps.meta.outputs.tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Deployment:** \`${{ steps.meta.outputs.deployment }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ✅ Docker image built successfully" >> $GITHUB_STEP_SUMMARY

  deploy:
    needs: build
    runs-on: self-hosted
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Determine deployment name
        id: deploy_info
        run: |
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "deployment=dashboard" >> $GITHUB_OUTPUT
          else
            echo "deployment=dashboard-develop" >> $GITHUB_OUTPUT
          fi

      - name: Restart deployment
        run: |
          microk8s.kubectl rollout restart deployment/${{ steps.deploy_info.outputs.deployment }} -n default
          microk8s.kubectl rollout status deployment/${{ steps.deploy_info.outputs.deployment }} -n default --timeout=5m
        continue-on-error: false

      - name: Deployment Summary
        run: |
          echo "### Deployment Summary :rocket:" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Deployment:** \`${{ steps.deploy_info.outputs.deployment }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ✅ Pods restarted and rolled out successfully" >> $GITHUB_STEP_SUMMARY
